# /code-review scratchpad output
CODE_REVIEW.md

# OS detritus
.DS_Store
*~
*.swp

# Local Claude Code state (per-machine, not for the project)
.claude/settings.local.json
.claude/agents/
.claude/plugins/

# Node — only present transiently if a maintainer runs `npm install` for
# Changesets. The repo itself ships no node code.
node_modules/
package-lock.json
